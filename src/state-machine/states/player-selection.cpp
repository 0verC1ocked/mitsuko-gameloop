#include "../../server.h"

void PlayerSelection::preUpdate(TransitionalCondition &condition, MatchModel &match_model,  ArenaAllocator &allocator) {
}
void PlayerSelection::onUpdate(TransitionalCondition &condition, MatchModel &match_model,  ArenaAllocator &allocator) {
  
}

void PlayerSelection::postUpdate(TransitionalCondition& condition, MatchModel &match_model,  ArenaAllocator &allocator) {
}

void PlayerSelection::autoPlay(TransitionalCondition &condition, MatchModel &match_model, ArenaAllocator &allocator) {   

}

void PlayerSelection::botPlay(TransitionalCondition &condition, MatchModel &match_model, ArenaAllocator &allocator) {   

}
