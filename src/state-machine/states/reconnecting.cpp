#include "../../server.h"

void Reconnecting::preUpdate(TransitionalCondition &condition, MatchModel &match_model,  ArenaAllocator &allocator) {
}
void Reconnecting::onUpdate(TransitionalCondition &condition, MatchModel &match_model,  ArenaAllocator &allocator) {
  
}

void Reconnecting::postUpdate(TransitionalCondition& condition, MatchModel &match_model,  ArenaAllocator &allocator) {
}

void Reconnecting::autoPlay(TransitionalCondition &condition, MatchModel &match_model, ArenaAllocator &allocator) {   

}

void Reconnecting::botPlay(TransitionalCondition &condition, MatchModel &match_model, ArenaAllocator &allocator) {   

}
